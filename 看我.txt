Session
Session 負責紀錄在 web server 上的使用者訊息。Session 機制會在一個用戶完成身分認證後，存下所需的用戶資料
，接著產生一組對應的 id，存入 cookie 後傳回用戶端

Cookie
最常見到的 Cookie 應用是在表單填寫：假設現在頁面上的資料填到一半，不小心把網頁關掉，
這時再重新打開發現先前填的內容還在的話，靠的就是 cookie。
Cookie 是一種小型的文字檔案，用途是在網頁瀏覽器上儲存資訊。
Cookie 可以在電腦、手機及其他裝置上，儲存和接收識別碼與其他資訊。其他技術，
包括我們儲存在您網頁瀏覽器或裝置上的資料、和您裝置相關聯的識別碼以及其他軟體，
也是用於類似的用途。這些技術在本政策中統稱為「Cookie」。

session：帳號登錄驗證過後，Server端所發的識別證
cookie：是瀏覽器存放資料的地方，可以存放seesion之類的資料 or 蒐集使用者資訊喜好並刊登廣告或推薦商品等等

//反轉字串1 C++
string str;  
    cout << "輸入一個字串: ";  
    cin >> str;  
    reverse(str.begin(),str.end());  
     cout << "該子串的反向: ";  
    cout << str;

//反轉字串2 C++
 int i,ilength;  
    char str1[20];  
    cout << "輸入一個字串: ";  
    cin >> str1;  
    ilength = strlen(str1);  
    cout << "該子串的反向:";  
    for(i = ilength;i>=1;i--){  
        cout << str1[i];  
    }  
    cout << str1[0] << endl;
	
什麼是MVC？
MVC - Model、View、Controller的縮寫。MVC並不是一個技術，而比較像是一種軟體開發的架構與邏輯，
不論網頁是使用PHP網頁還是ASP網頁開發，都可使用相同的邏輯。
MVC把系統分為三個基本部分：
Model-負責邏輯與資料處理
View-負責UI介面
Controller-負責接收請求，協調Model與View回應結果。

API 是什麼? API 就是你的餐廳服務生
API，全名 Application Programming Interface (應用程式介面)，簡單來說，是品牌開發出的一種接口，
讓第三方可以額外開發、應用在自身的產品上的系統溝通介面。

網頁狀態碼(status code)
2xx = Success（成功）
3xx = Redirect（重定向）
4xx = User error（客戶端錯誤）
5xx = Server error（伺服器端錯誤）

REST，全名 Representational State Transfer( 表現層狀態轉移)，他是一種設計風格，
RESTful 只是轉為形容詞，像是 peace 和平這名詞，轉成形容詞是 peaceful，RESTful 則形容以此規範設計的 API，
稱為 RESTful API。
先用白話來說，以剛剛 API 影片中的餐廳服務生為例，
如果使用一般的 API 點菜，我要加點、查看已點菜色、修改已點菜色、取消已點菜色，
都需要不同的服務生替我服務，可能加點的服務生是 John、查看已點菜色是 Annie….等，RESTful API，
就是讓這些動作，都可以由同一位服務生完成。

URL  Uniform Resource Locator，中文學術名詞是網域名稱 (Domain name)
WWW：主機名稱，或稱子網域、次網域( Subdomain )World Wide Web
FTP (檔案傳輸協定)
mailto (電子郵件傳輸)

com標示你的網站是屬於怎樣性質，比較常見的有 com (公司行號或營利單位)、
gov(政府機關)、edu(學術機構或教育單位)、net(網路組織)

tw：在台灣的網站其網址結尾處大都會以 .tw 做為結尾，其意義代表的是國別，
也可以說網址申請的機構所在的國家，而在用途上標示 .tw 可以讓使用者知道你的網站所在地是台灣，而非其它國家。

IP是 Internet Protocol Address的縮寫，意思是網際網路協定位址，
每個網站或網頁都有一組獨一無二的IP位址，看起來就像 203.11.65.4
將IP位址轉換網域名稱以及DNS(網域名稱伺服器  Domain Name Server )的技術，
自此以後人們在使用網路時不用再使用冗長的數字串，而是可以透過DNS轉換IP位址成為容易識別的英文字段。

SEO，也就是Search Engine Optimization，中文稱作搜尋引擎優化。

.wiki-inner
{
	line-height:1.6;
	margin:12px auto;
	max-width:600px;
	width:90%
}

在<picture>結束前指定一個<img>
最後再指定一個<img>元素作為<picture>標籤中的預設圖片、
當瀏覽器不支援<picture>標籤或是沒有符合的<source>時，則直接顯示<img> 。

一般而言，正規化的精神就是讓資料庫中重複的欄位資料減到最
少，並且能快速的找到資料，以提高關聯性資料庫的效能。

第一正規化(1NF)是指在資料表中的所有記錄之屬性內含值都是基元值(Atomic Value)。亦即無重覆項目群。
第二正規化(2NF)在完成了第一正規化之後，是否發現在資料表中產生許多重複的資料。如此, 不但浪費儲存的空間, 更容易造成新增、修改及刪除資料時的異常狀況
第三正規化(3NF)各欄位與「主鍵」之間沒有「遞移相依」的關係。































